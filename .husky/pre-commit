#!/usr/bin/env sh
# Check-only version of pre-commit hook
# This will check for issues but NOT modify your code

echo "🔍 Running pre-commit checks (read-only)..."

# Check formatting without fixing
yarn check:format

# Check linting without fixing
# cd next && npx eslint . --ext .js,.jsx,.ts,.tsx

echo "✅ Pre-commit checks completed (no code was modified)"
