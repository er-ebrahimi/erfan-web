/** Central revalidation configuration for the entire app.
 *  Pages should mirror this value in their `export const revalidate` literal.
 *  fetchContentType imports this directly as its default. */
export const REVALIDATE_SECONDS = 60;
